{
  "compilerOptions": {
    "lib": [
      "es6"
    ],
    "target": "es6", // Choisis ta cible ECMAScript
    "module": "commonjs", // Type de module, 'commonjs' pour les applications Node.js
    "outDir": "./dist", // Dossier où les fichiers JS transpilés seront placés
    "rootDir": "./", // Dossier racine de tes fichiers TS
    "strict": true, // Active des contrôles de type plus stricts
    "esModuleInterop": true, // Permet l'interopérabilité des modules ES6 avec CommonJS
    "skipLibCheck": true, // Ignore les vérifications de types pour les fichiers de déclaration
    "forceConsistentCasingInFileNames": true // Force la cohérence des majuscules et minuscules dans les noms de fichiers
  },
  "include": [
    "./**/*",
  ], // Inclut tous les fichiers TS du projet
  "exclude": ["node_modules"] // Exclut le dossier 'node_modules'
}
